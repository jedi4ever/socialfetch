# Build artifacts
/dist/
/bin/
# The bundled skill binary is a build product; SKILL.md stays committed.
skill/social-fetch/scripts/social-fetch

# Local-only npm tooling (currently just @anthropic-ai/mcpb for
# validating the Claude Desktop Extension manifest). package.json +
# package-lock.json are committed; the install tree is not.
/node_modules/

# Go test outputs
*.test
*.out
coverage.out

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local environment — never commit credentials
.env
.env.*
**/.env
**/.env.*
*.local

# Output / scratch
/out/
*.log
/social-fetch

# Bundled skill ledger binary — built fresh by `make skill-build`,
# same gitignored treatment as scripts/social-fetch.
skill/social-fetch/scripts/social-ledger
skill/social-ledger/scripts/social-ledger
