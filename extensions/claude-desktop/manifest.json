{
  "manifest_version": "0.3",
  "name": "social-skills",
  "display_name": "social-skills — for AI agents",
  "version": "0.10.3",
  "description": "Social-web skills for AI agents. Bundles HackerNews / Reddit / GitHub / X / LinkedIn / YouTube / Bluesky / arXiv / Medium / Substack / RSS / generic article fetchers, plus search (DuckDuckGo, Brave, SerpAPI, Tavily, Perplexity, X, HN, Reddit, YouTube, Bluesky, arXiv, Google CSE), ask (Perplexity, Grok, OpenAI, Anthropic, Gemini, Tavily, SerpAPI), and a local SQLite-backed ledger of every fetched URL.",
  "long_description": "social-skills equips AI agents with structured access to the social web. Bundles two binaries: social-fetch (MCP server exposing fetch / search / ask / timeline / research / list_providers / bridge_status as tools) and social-ledger (auto-detected SQLite + FTS5 store that caches every fetch so 'have we seen this URL?' answers are instant without re-fetching). Provider chains (-p auto) gracefully fall through providers that lack keys or hit rate limits, so you can ship with whichever subset of API keys you've configured and the rest is silent.",
  "author": {
    "name": "Patrick Debois"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jedi4ever/social-skills"
  },
  "homepage": "https://github.com/jedi4ever/social-skills",
  "documentation": "https://github.com/jedi4ever/social-skills/blob/main/INSTALL.md",
  "support": "https://github.com/jedi4ever/social-skills/issues",
  "keywords": [
    "agent-skills", "ai-agents", "mcp", "claude", "anthropic",
    "social-media", "search", "fetch", "ledger", "research", "grounded-answers",
    "hackernews", "reddit", "github", "twitter", "x", "linkedin",
    "youtube", "bluesky", "arxiv", "medium", "substack", "rss"
  ],
  "license": "MIT",
  "server": {
    "type": "binary",
    "entry_point": "scripts/social-fetch",
    "mcp_config": {
      "command": "${__dirname}/scripts/social-fetch",
      "args": ["mcp"],
      "env": {
        "ANTHROPIC_API_KEY": "${user_config.anthropic_api_key}",
        "OPENAI_API_KEY": "${user_config.openai_api_key}",
        "PERPLEXITY_API_KEY": "${user_config.perplexity_api_key}",
        "XAI_API_KEY": "${user_config.xai_api_key}",
        "GOOGLE_API_KEY": "${user_config.google_api_key}",
        "GOOGLE_CSE_ID": "${user_config.google_cse_id}",
        "TAVILY_API_KEY": "${user_config.tavily_api_key}",
        "SERPAPI_KEY": "${user_config.serpapi_key}",
        "BRAVE_API_KEY": "${user_config.brave_api_key}",
        "X_API_KEY": "${user_config.x_api_key}",
        "X_API_SECRET": "${user_config.x_api_secret}",
        "YOUTUBE_API_KEY": "${user_config.youtube_api_key}",
        "GITHUB_TOKEN": "${user_config.github_token}",
        "BLUESKY_HANDLE": "${user_config.bluesky_handle}",
        "BLUESKY_APP_PASSWORD": "${user_config.bluesky_app_password}",
        "JINA_API_KEY": "${user_config.jina_api_key}",
        "HTML2MD_PROVIDER": "${user_config.html2md_provider}",
        "HTML2MD_READER": "${user_config.html2md_reader}",
        "TAVILY_TOPIC": "${user_config.tavily_topic}",
        "SOCIAL_LEDGER": "${user_config.social_ledger}",
        "SOCIAL_LEDGER_DIR": "${user_config.social_ledger_dir}",
        "SOCIAL_LEDGER_READONLY": "${user_config.social_ledger_readonly}",
        "SOCIAL_FETCH_AUDIT": "${user_config.social_fetch_audit}",
        "SOCIAL_BRIDGE_URL": "${user_config.social_bridge_url}"
      }
    }
  },
  "tools": [
    {"name": "social_fetch_fetch", "description": "Fetch content at a URL (HN, Reddit, GitHub, X, LinkedIn, YouTube, Bluesky, arXiv, Medium, Substack, RSS, generic)."},
    {"name": "social_fetch_search", "description": "Search the web or a specific platform. Supports -p auto fallback chains."},
    {"name": "social_fetch_ask", "description": "Ask a grounded answer engine (Perplexity, Grok, OpenAI, Anthropic, Gemini, Tavily, SerpAPI)."},
    {"name": "social_fetch_timeline", "description": "Recent activity for a user on X or LinkedIn."},
    {"name": "social_fetch_list_providers", "description": "Discover available providers at runtime."},
    {"name": "social_fetch_bridge_status", "description": "Probe the local browser bridge (LinkedIn / Medium / Substack)."},
    {"name": "social_fetch_research", "description": "EXPERIMENTAL — multi-angle research workflow: decomposes a question, fans out parallel fetch/search/ask/timeline, synthesizes a markdown answer with citations."},
    {"name": "social_ledger_seen", "description": "Check whether one or more URLs are already cached in the local ledger. Use BEFORE fetching to avoid re-fetching content already saved."},
    {"name": "social_ledger_get", "description": "Retrieve one stored item from the ledger by URL — full markdown body + frontmatter."},
    {"name": "social_ledger_list", "description": "List ledger items, newest first. Filter by source / recency window."},
    {"name": "social_ledger_search", "description": "FTS5 search the ledger (BM25-ranked) across title / summary / content / author / tags."},
    {"name": "social_ledger_stats", "description": "Counts, sizes, oldest/newest in the ledger."},
    {"name": "social_ledger_record", "description": "Store one URL+content pair in the ledger (e.g. after a Claude WebFetch). Refused when SOCIAL_LEDGER_READONLY=1."},
    {"name": "social_ledger_forget", "description": "Drop one item from the ledger. Refused when SOCIAL_LEDGER_READONLY=1."}
  ],
  "compatibility": {
    "platforms": ["darwin"]
  },
  "user_config": {
    "anthropic_api_key": {
      "type": "string",
      "title": "Anthropic API key",
      "description": "Claude ask provider — get one at console.anthropic.com",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "openai_api_key": {
      "type": "string",
      "title": "OpenAI API key",
      "description": "OpenAI ask provider — get one at platform.openai.com",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "perplexity_api_key": {
      "type": "string",
      "title": "Perplexity API key",
      "description": "Perplexity ask + search providers — perplexity.ai/settings/api",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "xai_api_key": {
      "type": "string",
      "title": "xAI / Grok API key",
      "description": "Grok ask provider — console.x.ai",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "google_api_key": {
      "type": "string",
      "title": "Google API key (Gemini + YouTube + CSE)",
      "description": "One key powers Gemini ask, YouTube, and Google search — console.cloud.google.com",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "google_cse_id": {
      "type": "string",
      "title": "Google Custom Search Engine ID",
      "description": "Required for the `google` search provider. Get from programmablesearchengine.google.com",
      "sensitive": false,
      "required": false,
      "default": ""
    },
    "tavily_api_key": {
      "type": "string",
      "title": "Tavily API key",
      "description": "Tavily ask + search — tavily.com (1k searches/mo free)",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "serpapi_key": {
      "type": "string",
      "title": "SerpAPI key",
      "description": "SerpAPI search + Google AI Overview ask — serpapi.com (100/mo free)",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "brave_api_key": {
      "type": "string",
      "title": "Brave Search API key",
      "description": "Brave search — api.search.brave.com (2k/mo free)",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "x_api_key": {
      "type": "string",
      "title": "X / Twitter API key",
      "description": "X API key (paired with secret) — developer.x.com. NOT the bearer token.",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "x_api_secret": {
      "type": "string",
      "title": "X / Twitter API secret",
      "description": "X API secret",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "youtube_api_key": {
      "type": "string",
      "title": "YouTube Data API key",
      "description": "Optional — YouTube fetch metadata works without it; needed for comments + search.",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "github_token": {
      "type": "string",
      "title": "GitHub token",
      "description": "Optional — raises rate limit from 60/hr to 5,000/hr.",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "bluesky_handle": {
      "type": "string",
      "title": "Bluesky handle",
      "description": "Required for Bluesky search (e.g. you.bsky.social). Bluesky fetch works without it.",
      "sensitive": false,
      "required": false,
      "default": ""
    },
    "bluesky_app_password": {
      "type": "string",
      "title": "Bluesky app password",
      "description": "Generate at bsky.app/settings/app-passwords — NOT your account password.",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "jina_api_key": {
      "type": "string",
      "title": "Jina API key (optional)",
      "description": "Optional — Jina Reader free tier works without a key. Set for higher rate limits.",
      "sensitive": true,
      "required": false,
      "default": ""
    },
    "html2md_provider": {
      "type": "string",
      "title": "HTML→Markdown converter",
      "description": "kaufmann (default, library-backed) or builtin (legacy hand-roll)",
      "sensitive": false,
      "required": false,
      "default": "kaufmann"
    },
    "html2md_reader": {
      "type": "string",
      "title": "HTML→Markdown reader",
      "description": "local (default) or jina (route generic article fetches via r.jina.ai — handles Cloudflare-blocked / JS-rendered pages)",
      "sensitive": false,
      "required": false,
      "default": "local"
    },
    "tavily_topic": {
      "type": "string",
      "title": "Tavily topic",
      "description": "Set to 'news' for strict date filtering (narrower recall). Empty/general for default.",
      "sensitive": false,
      "required": false,
      "default": ""
    },
    "social_ledger": {
      "type": "string",
      "title": "Ledger mode",
      "description": "auto / 1 / 0. Default `auto` enables the ledger when social-ledger is bundled (always true in this .mcpb). `1` = explicit on; `0` = explicit off. The ledger caches every fetch in SQLite + FTS5 so 'have we seen this URL?' is instant.",
      "sensitive": false,
      "required": false,
      "default": "auto"
    },
    "social_ledger_dir": {
      "type": "string",
      "title": "Ledger storage directory",
      "description": "Override where social-ledger stores its SQLite + mirror tree. Default: $XDG_DATA_HOME/social-ledger or ~/.local/share/social-ledger.",
      "sensitive": false,
      "required": false,
      "default": ""
    },
    "social_ledger_readonly": {
      "type": "string",
      "title": "Ledger read-only mode",
      "description": "`1` disables ledger writes (record / forget / auto-ingest). Reads (seen / get / list / search / stats) keep working. Default `0` = writes allowed. Useful for research-only agent profiles that should consult but not mutate the ledger.",
      "sensitive": false,
      "required": false,
      "default": "0"
    },
    "social_fetch_audit": {
      "type": "string",
      "title": "Audit log",
      "description": "`0` disables the always-on audit log; `1` (default) enables it. Audit lines record every fetch / MCP tool call for `social-fetch monitor`.",
      "sensitive": false,
      "required": false,
      "default": "1"
    },
    "social_bridge_url": {
      "type": "string",
      "title": "Browser-bridge URL",
      "description": "Local Chrome-bridge daemon endpoint. Default: http://127.0.0.1:5555/cmd. Change when running multiple sandboxed browser profiles or forwarding to a remote machine.",
      "sensitive": false,
      "required": false,
      "default": "http://127.0.0.1:5555/cmd"
    }
  }
}
